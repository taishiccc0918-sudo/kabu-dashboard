-- ============================================================
-- memos テーブルに「ジャンル」列を追加（ジャンルをクラウド保存＝端末/ドメイン非依存に）
-- Supabase ダッシュボード → SQL Editor で1回だけ実行してください。
-- これでメモと同じく、ジャンルもクラウドに残り自動バックアップされます。
-- ============================================================

alter table public.memos
  add column if not exists genres jsonb not null default '[]'::jsonb;
